import { AdminCustomPlanRequests } from "@/components/admin/AdminCustomPlanRequests";

export const metadata = {
  title: "Custom plan requests — Admin",
};

export default function AdminCustomPlanRequestsPage() {
  return <AdminCustomPlanRequests />;
}
