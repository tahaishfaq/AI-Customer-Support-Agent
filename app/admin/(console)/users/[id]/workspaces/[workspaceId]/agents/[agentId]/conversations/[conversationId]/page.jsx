import { AdminConversationThread } from "@/components/admin/AdminConversationThread";

export const metadata = {
  title: "Conversation — Admin",
};

export default function AdminConversationPage() {
  return <AdminConversationThread />;
}
